<%= form_with(model: language, local: true) do |form| %>
  <% if language.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(language.errors.count, "error") %> prohibited this language from being saved:</h2>

      <ul>
      <% language.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_area :name %>
  </div>

  <div class="field">
    <%= form.label :localised_name %>
    <%= form.text_area :localised_name %>
  </div>

  <div class="field">
    <%= form.label :iso2 %>
    <%= form.text_area :iso2 %>
  </div>

  <div class="field">
    <%= form.label :iso3 %>
    <%= form.text_area :iso3 %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
